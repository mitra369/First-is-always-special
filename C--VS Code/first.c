#include <stdio.h>
int main()
{
    int p;
    scanf("%d", &p);
    int t=1;
    while(t)
    {
        if(t==5)
        {
            printf("hello");
            break;
        }
    printf("%d", p*55);
    t++;
    }
}